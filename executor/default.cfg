[Kubernetes]
token_file = /root/.kube/config

[VirtualMachine]
plural = virtualmachines
group = cloudplus.io
version = v1alpha3

[SupportCmds]
install = virt-install
attach_interface = virsh attach-interface
detach_interface = virsh detach-interface