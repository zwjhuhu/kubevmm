{
  "apiVersion": "cloudplus.io/v1alpha3",
  "kind": "VirtualMachineSnapshot",
  "metadata": {
    "name": "disk1"
  },
  "spec": {
    "image": "CentOS7",
    "nodeName": "localhost.localdomain",
    "lifecycle": {
      "createSnapshot": {
        "domain": "default",
      }
    }
  }
}
